# Overrides the `.gitignore` file since we want to publish the `lib/` dir.

# Intermediate binaries that are later bundled into an XCFramework
/lib/aarch64-apple-ios/libmedea_jason.a
/lib/x86_64-apple-ios/libmedea_jason.a
/lib/aarch64-apple-ios-sim/libmedea_jason.a
